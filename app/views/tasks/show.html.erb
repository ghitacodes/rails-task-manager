<p><%= @task.title %></p>
<p><%= @task.details %></p>
<%= link_to("Edit task", edit_path) %>
<%= link_to("Delete task", delete_path(@task), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}) %>
<%= link_to("Back to title", root_path) %>
